{
  "name": "case50-gmail-backend",
  "version": "1.0.0",
  "description": "Gmail OAuth backend for Case50 iOS app",
  "main": "api/index.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  }
}